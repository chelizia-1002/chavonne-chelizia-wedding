const btn=document.getElementById('menuBtn'); const menu=document.getElementById('mobileMenu');
btn?.addEventListener('click',()=>menu.classList.toggle('open')); menu?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>menu.classList.remove('open')));
const target=new Date('2026-11-14T15:00:00+02:00');
function tick(){const now=new Date(); let diff=Math.max(0,target-now); const d=Math.floor(diff/86400000); diff-=d*86400000; const h=Math.floor(diff/3600000); diff-=h*3600000; const m=Math.floor(diff/60000); diff-=m*60000; const s=Math.floor(diff/1000); const vals=[d,h,m,s]; document.querySelectorAll('#countdown strong').forEach((el,i)=>el.textContent=String(vals[i]).padStart(i?2:3,'0'));}
tick(); setInterval(tick,1000);
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in')}),{threshold:.12}); document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
document.getElementById('rsvpForm')?.addEventListener('submit',e=>{e.preventDefault(); const data=new FormData(e.currentTarget); const body=[`Name: ${data.get('name')}`,`Guests attending: ${data.get('guests')}`,`Dietary requirements: ${data.get('diet')||'None'}`,`Song request: ${data.get('song')||'None'}`,`Message: ${data.get('message')||''}`].join('%0D%0A'); window.location.href=`mailto:cheliziavdw@gmail.com?subject=Wedding RSVP - ${encodeURIComponent(data.get('name'))}&body=${body}`;});
